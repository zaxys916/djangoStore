[project]
name = "djangoStore"
version = "1.0.0"
description = "Django project"
requires-python = ">=3.8"
dependencies = [
    "django>=4.2,<5.0",
    "djangorestframework>=3.14",
    "gunicorn>=21.0",
]